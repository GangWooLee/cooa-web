<% content_for :title, "버전 편집 · #{@component.product.code}" %>
<div class="mx-auto w-full max-w-3xl p-6">
  <div class="mb-5">
    <div class="text-note"><%= node_breadcrumb(@component.product, trailing: "#{@component.display_name} #{@version.vlabel} 편집") %></div>
    <h1 class="mt-2 text-display font-bold text-ink"><%= @component.display_name %> <%= @version.vlabel %> 편집</h1>
  </div>
  <%= render "form" %>
</div>
